import { NgModule } from "@angular/core";
import { OpenDirective } from "./open.directive";

@NgModule({
    declarations: [ OpenDirective],
    exports:[OpenDirective],
  })
  export class OpenDirectiveModule { }